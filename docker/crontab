# 평일(월~금) 18:00 KST에 daily 수집(--days 7) + Drive 동기화 실행.
# 공시 데이터라 장마감 시각과 무관 - 시간은 필요에 맞게 조정 가능.
#
# root로 실행 - /proc/1/fd/1(컨테이너 stdout)이 root:root 전용 권한(l-wx------)이라
# nonroot로 돌리면 리다이렉션 자체가 Permission denied로 실패해 잡이 아예 안 돎.
0 18 * * 1-5 root /app/docker/run-crawl.sh >> /proc/1/fd/1 2>&1
